// Paleta dos gráficos validada computacionalmente (lightness band, piso de croma,
// separação CVD entre pares adjacentes e contraste >= 3:1 sobre superfície clara).
// Não substituir "a olho": revalidar qualquer troca antes de usar.
export const chartColors = {
  primaria: '#0891b2',
  secundaria: '#c2410c',
} as const;
